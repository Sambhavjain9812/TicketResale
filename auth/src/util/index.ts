export const filterData = (obj:object, allowedFields:Array<string>) => {
    // let newObj = {};
    // Object.keys(obj).forEach((el) => {
    //   if (allowedFields.includes(el)) newObj[el] = obj[el];
    // });
    // return newObj;
  };